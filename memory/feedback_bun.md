---
name: Use bun as package manager
description: User prefers bun over npm for installing packages
type: feedback
---

Use `bun add` instead of `npm install` for package management.

**Why:** User explicitly corrected when npm was used.
**How to apply:** Always use bun commands (bun add, bun install, bun run) in this project.
